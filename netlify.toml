[build]
  publish = "dist"
  command = "npm run build"

[dev]
  targetPort = 3000

[functions]
  # Allow larger file uploads for audio files

[build.environment]
  # Enable Node.js 18 for better compatibility
  NODE_VERSION = "18"
  # Set the site URL for OAuth callbacks
  SITE_URL = "https://myfreezer.netlify.app"
