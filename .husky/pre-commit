#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Run lint-staged (linting and formatting on staged files only)
# This allows WIP commits but keeps code formatted
npx lint-staged

# Run type checking
npm run type-check

# Test build to ensure it doesn't break
npm run build
